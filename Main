def count_vowels(word):
    vowels = 'aeiouAEIOU'
    # Counter for vowels
    count = 0
    for char in word:
        if char in vowels:
            count += 1
    return count

word= "Hello"
print(f"Vowels in '{word}' is: {count_vowels(word)}")
